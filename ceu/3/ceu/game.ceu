input _SDL_MouseButtonEvent* SDL_MOUSEBUTTONDOWN; 
input int SDL_DT;
#include "level.ceu"
class Game with
    event void ok;
do
    //l=0
    //spawn level with level = l end
    //l ++
    var int actual = 0;
    var Level leveL with
        this.level = actual;
    end;
    await FOREVER;
    
        /*
    every level.ok do
        actual++;
        spawn Level in levels with
            this.level = actual;
        end
    end
        */
end	
